---
# control
control:
  data_name: TwoMoons
  model_name: mlp
  formulation_mode: at
  is_time: 1
  is_cond: 0
  regularization:
    v: 1.
    x0: 0.
    x1: 0.
    consistency: 1.
# generate
generate:
  num_steps: 1
  guidance_scale: 2.
  eta: 0.
# experiment
pin_memory: True
num_workers: 0
init_seed: 0
num_experiments: 1
log_interval: 0.25
device: cuda
resume_mode: 0
profile: False
